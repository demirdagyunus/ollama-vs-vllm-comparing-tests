Each `scenario-{n}` directory stores chronologically stamped JSON + TXT artefacts.
Figure generators lexicographically select the freshest `*_data_*.json` match when duplicates exist — archive stale exports externally before deleting locally.
