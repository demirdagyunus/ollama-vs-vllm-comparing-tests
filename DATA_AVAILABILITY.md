# Data Availability Statement

The empirical traces supporting the manuscript comprise JSON exports under `cases/results/scenario-{1,2,3,4}`, with companion `*_report_*.txt` summaries. Each file stores per-run aggregates (latency, throughput, reasoning accuracy splits, TTFT summaries, endurance timelines).

For DOI archival, optionally mirror snapshots to Zenodo (placeholder DOI pending editorial assignment). Prefer citing the precise git commit packaged with supplementary materials.
